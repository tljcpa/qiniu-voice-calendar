# 语音日历后端依赖
# 版本采用兼容性区间下限锁定，保证 Python 3.10/3.11 均可安装。
# 各依赖随对应 PR 引入，PR1 仅需 Web 框架。

# --- Web 框架（PR1）---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# --- LLM 客户端（PR3）---
openai>=1.30,<2.0

# --- HTTP 客户端（PR4：token 签发用，运行时依赖）---
httpx>=0.27,<1.0

# --- Azure 语音（PR4）---
# 服务端 TTS/ASR 用；token 签发只用 httpx 不依赖此 SDK。
# 本机内存受限不强制安装，部署镜像与 VM 安装。
azure-cognitiveservices-speech>=1.37,<2.0

# --- 中文时间解析（PR6）---
# 仅作规则层未命中的边角兜底；实测中文能力弱（见复盘 L-04）。
dateparser>=1.2,<2.0

# --- 数据库 / 调度（PR7、PR14 启用）---
# SQLAlchemy>=2.0,<3.0
# APScheduler>=3.10,<4.0

# --- 测试 ---
pytest>=8.0,<9.0
