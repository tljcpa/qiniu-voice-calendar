"""语音日历后端应用包。

模块职责（随 PR 逐步填充）：
- config.py        运行期配置（从环境变量读取，集中管理）
- main.py          FastAPI 应用入口与路由装配
- 后续 PR 加入：llm_provider / speech / intent_parser / time_parser / models / crud / conflict
"""

__version__ = "0.1.0"
