# Caddy 反向代理 + 自动 HTTPS（占位骨架，PR13 完善）。
#
# 为什么用 Caddy：自动申请并续期 Let's Encrypt 证书，零配置 HTTPS。
# 浏览器麦克风权限要求安全上下文（HTTPS），上线必须有证书——这是硬约束。

voice.qiniu.zdwktlj.top {
	# 反代后端 API
	# reverse_proxy /api/* backend:8081

	# 托管前端静态产物
	# root * /srv/frontend
	# file_server

	# PR13 取消注释并补齐
	respond "voice-calendar deploy placeholder" 200
}
